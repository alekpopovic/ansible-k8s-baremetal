---
extends: default

ignore: |
  templates/*.j2
  roles/*/templates/*.j2

rules:
  line-length:
    max: 100
    level: warning
  truthy:
    check-keys: false
    allowed-values:
      - "true"
      - "false"
